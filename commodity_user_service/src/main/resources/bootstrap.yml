
spring:

  cloud:
    #Config相关配置
    config:
      discovery:
        enabled: true
        #Config-server的服务名
        service-id: config_server
      #环境
      profile: dev
      #分支
      label: master